DB_HOST=localhost
DB_PORT=5432
DB_NAME=eventfrog_screen
DB_USER=postgres
DB_PASSWORD=postgres
# Nur lokal – auf dem Server kommt der Port aus GitHub (Variable APP_PORT)
PORT=3000
# Erzeugen: openssl rand -hex 32
SESSION_SECRET=
NODE_ENV=production
